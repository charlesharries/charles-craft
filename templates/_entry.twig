{% extends "_layout.twig" %}

{% block content %}
{% set entry = craft.entries.slug(slug).first() %}
<main class="container">
    <h1>{{ entry.title }}</h1>

    {{ entry.body }}
</main>
{% endblock content %}